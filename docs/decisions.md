# Decisions log

This file records decisions taken where the spec or the master prompt was silent, or where the
build had to deviate from them. Each entry has a one-line reason. The same entries appear in
`PROGRESS.md` → the "Decisions made in Phase N" section for that phase.

## Phase 0 (17 Sep 2026)

| # | Decision | Reason |
| --- | --- | --- |
| 0-1 | Laravel **13** (13.32 at build time), not the spec's 11 | Laravel 11 no longer gets security fixes; the client confirms at GATE A |
| 0-2 | Vue pages and components are written in **TypeScript** (`<script setup lang="ts">`) | shadcn-vue components are TypeScript-first; the type check (`vue-tsc`) catches prop-contract drift |
| 0-3 | Tests run as **hq_app**; the schema is built as hq_migrator (`tests/Concerns/RefreshHqDatabase`) | Stricter than master prompt B§3 (which runs tests as the migrator): the tests exercise the real runtime grants |
| 0-4 | `user_project_permissions.project_id` has no foreign key until Phase 1 | The `projects` table does not exist yet; Phase 1 adds the FK |
| 0-5 | The MANAGER role lands in the **Employee** shell | MANAGER is dormant and has no UI in the MVP, so it gets the least-privilege shell |
| 0-6 | Default working week is **Sun–Thu, 09:00, 8 h** for office staff; Tapu's is Sun–Thu at 5 h, remote | The spec is silent; the client confirms at GATE A |
| 0-7 | Seeded logins are `*@goodtechies.test`; addresses can be overridden with `SEED_*_EMAIL`, passwords come from `SEED_PASSWORD` | No passwords or emails are hard-coded in the repo |
| 0-8 | Design tokens were derived by hand into DESIGN.md and `app.css` | The shadcn-tokens MCP was not reachable in the build session; its `get_theme` should read `app.css` back |
| 0-9 | Icons come from `@lucide/vue` | `lucide-vue-next` is deprecated upstream, and the shadcn-vue CLI generates `@lucide/vue` imports |
| 0-10 | Builds need **Node 22** (install.sh, CI) | The lockfile's packages require Node ≥ 22, and Node 20 is end-of-life |
| 0-11 | Admin → Settings is **read-only** in Phase 0, including the Backup health card | Settings editing belongs to Phase 12 |
| 0-12 | Login is throttled at 5/min per email + IP; the 2FA challenge at 5/min per pending user + IP | Brute-force protection (spec §36 rate limiting) |
| 0-13 | Password rule: at least 12 characters plus the HIBP k-anonymity breach check, no composition rules | Spec §36 |
| 0-14 | 2FA: TOTP with a ±30 s window and replay protection; 8 recovery codes, stored hashed and usable once | Spec §36 |
| 0-15 | The DB session timezone follows `DB_TIMEZONE` (default Asia/Dhaka) | Keeps `timestamptz` columns consistent with the app timezone on a UTC server |
| 0-16 | Backups dump the **database only** (as hq_migrator), encrypted with AES-256; hq_migrator gets CREATEDB for the `hq:verify-backup` scratch DB | Files are protected by bucket versioning and replication (master prompt B§4); the restore test needs a scratch database |
| 0-17 | The `hq-reverb` Supervisor program ships with `autostart=false` | Reverb is installed in Phase 6 |
| 0-18 | Changing the password rotates `remember_token` and deletes the user's other sessions | Otherwise a stolen device stays signed in |
| 0-19 | In Phase 0, "Active employees" is the only real number on the Admin dashboard; every other card is labelled "Arrives in Phase N" | No fake UI (master prompt 0.5) |
| 0-20 | Profile email is lower-cased on save | Login looks users up case-insensitively, so two accounts must never differ only by case |

## Phase 1 (20 Sep 2026)

| # | Decision | Reason |
| --- | --- | --- |
| 1-1 | An internal project has `client_id = null`; there is no "GoodTechies" client row | The spec calls a project without a client "Internal"; a placeholder client would show up in every client list |
| 1-2 | A project's status moves only through `POST …/status`, `…/archive`, `…/unarchive`; `PUT /admin/projects/{id}` ignores a `status` key | The security critic found that the general update route let an assigned MANAGER cancel or reopen a project and leave `archived_at` unset |
| 1-3 | MANAGER reaches no `/admin/*` route (it lands on the Employee shell, decision 0-5), so manager-scoped project editing is not usable in the MVP | MANAGER is assigned to nobody and has no UI until Phase 12; the policies still carry the scoped rules |
| 1-4 | The employee project list sorts by deadline with nulls last, then by name | A retainer without a deadline should not sit above work that is due |
| 1-5 | The UI formats money as USD in one small helper per screen | `settings.currency` is not yet a prop; the helper carries a comment to move it when it is |
| 1-6 | Demo dates are relative to `today()`, and one project is deliberately overdue and one has no deadline | The seed has to keep reading well and exercise the overdue styling |
| 1-7 | No "Internal only" option in the client filter yet | The server reads `client_id` numerically; adding a sentinel is a server change, parked as a follow-up |
| 1-8 | A member's role on a project is edited only on the project page, not in the create form | The store request validates member ids only |
| 1-9 | `ProjectResource` does not expose `created_at` | Nothing needs it yet, and every field it carries is a field to review for privacy |
| 1-10 | The employee project page marks "You" by matching the viewer's name | The shared props carry the user id, not the employee id; switch it when Phase 2 adds one |

## Phase 0.5 (20 Sep 2026) — Design Foundation

Spec: `docs/design-foundation-v1.md` (v1.1). Brand source of truth:
`docs/design-refs/GoodTechies siteicon  1.svg`, which holds exactly three colours and no
gradient — `#0A0D12` (tile), `#FFFFFF` (glyphs), `#F04E27` (one dot). Token implementation:
`resources/css/app.css`.

**A note on the numbering.** The spec's §7 reserved `0.5-1` … `0.5-7` for the seven decisions
the phase had to *close*. T1 was written first and reused those numbers for its token choices,
so four of them meant two different things at once. The reserved numbers win, because the code
cites them with the spec's meaning (`app.css:75` → 0.5-1, `navigation/types.ts:39` → 0.5-3,
`app.css:57` → 0.5-4, `TimerHeroCard.vue:9` → 0.5-5). T1's seven colliding rows moved to
**0.5-21 … 0.5-27**, in their original order and with their original wording. Nothing was
deleted and no number now means two things.

| # | Decision | Reason |
| --- | --- | --- |
| 0.5-1 | The shell is the **light neutral** model (Option A), not full dark (Option B) | The user chose it. An agency tool is read all day beside documents and a browser; a dark chrome around light content makes the app the loudest thing on the screen |
| 0.5-2 | ~~`--brand` exact value~~ **Closed.** `#F04E27` and `#0A0D12`, read directly out of `GoodTechies siteicon 1.svg`; the neutrals move to the logo ink's hue 260.6 | v1 of the spec sampled the colours from a raster with an orange gradient that the client has since replaced. There is no sampling left to do — the SVG states them |
| 0.5-3 | Unbuilt nav rows live in one closed **Coming soon** disclosure at the bottom of the sidebar, not in their groups and not on a Settings page | It keeps the roadmap visible without letting 29 dead rows outweigh the live ones; a row goes live by dropping its `phase` key |
| 0.5-4 | Charts are **unovis** (`@unovis/vue`); Phase 10's Gantt is a custom component, not a chart | The user chose it: Vue-native, reads our CSS variables, and light enough that no phase is tempted to add a second library |
| 0.5-5 | The employee timer is a **dashboard hero card**, not a header widget | The user chose it; the dashboard is the first screen after login and the timer is the one thing a remote day starts with |
| 0.5-6 | The default Tasks view is a **List grouped by status**, not a Board | The user chose it. It also settles Phase 2's first build: `DataTable` with grouping, not a Kanban — the Board can come later as a second view over the same data |
| 0.5-7 | The lockup is the mark plus "GoodTechies HQ" in Inter Semibold, cap-height matched; **open** until the client says whether a real wordmark is coming | There is no wordmark in the supplied artwork, and inventing one would be a brand decision, not a build decision |
| 0.5-8 | Dark status triplets use a **uniform mirror** — fg at L 0.80, bg at L 0.26, border at L 0.38 — rather than a literal fg↔bg lightness swap | Measured: the literal swap gives `cancelled` **3.65:1 (FAIL)** and collapses hue identity (progress bg `#495762` vs done bg `#4A5A4D` are indistinguishable). The uniform mirror holds every pair at 6.47–8.65:1 |
| 0.5-9 | The `--status-todo` dot moves to the neutral ramp (0.70 / 260.6) and the `--status-progress` dot to its own badge hue 240 | Both were Phase 0 leftovers — slate at hue 227.7 and teal at 195.1 — so a teal dot sat inside a blue badge and a 223-family grey survived the ramp change |
| 0.5-10 | `.dark` gets its own deeper `--elevation-raised` / `--elevation-overlay` (0.30/0.36 and 0.50 black) | A 5 % black shadow is invisible on a `#0A0D12` canvas, so the light-mode values would silently flatten every card and dialog in dark mode |
| 0.5-11 | ~~`--sidebar` / `--sidebar-primary` / `--sidebar-ring` keep their Phase 0 navy and teal~~ **Superseded by 0.5-1.** T1 deferred the shell colour to T2 and left the Phase 0 values in place; T2 then chose Option A and replaced all of them | The reason for deferring still stands — a token task may not pre-empt a shell decision — but the values it preserved are gone. `app.css` is the current answer |
| 0.5-12 | `--destructive` is left at the shadcn default in both modes | Out of scope for T1. Measured as rendered (`dark:bg-destructive/60` over `--card`) it passes at **6.00:1**; the raw token under white text is 2.89:1, which the app never renders |
| 0.5-13 | Trailing hexes in `app.css` are the sRGB render of the oklch value, verified token by token | Several spec hexes are out of gamut and clip differently (`--primary` renders `#D12D00`, the spec quotes `#CF3100`); the in-gamut neutral ramp matches the spec to the digit, confirming the conversion |
| 0.5-14 | `--primary-hover` in dark mode is **lighter** than `--primary` (L 0.78 vs 0.72), not darker | On a near-black canvas hover has to move away from the background, which is the opposite direction to light mode's L 0.50 |
| 0.5-15 | The bare mark's glyphs stay white rather than `currentColor` | It is only used on the near-black brand ink, where white is 21:1 and no caller can break it |
| 0.5-16 | `DataTable` ships sorting and rows-per-page, but both stay disabled until a controller accepts `sort`, `dir` and `per_page` | Phase 0.5's own non-goals forbid touching PHP; turning them on is a one-line change per list once the server learns them |
| 0.5-17 | The five chart hues are 34.43 (brand) / 131 / 212 / 265 / 329, with their own dark steps | Checked with a deuteranopia and protanopia simulation, not by eye; every hue also sits clear of the six status hues so a category cannot read as a status |
| 0.5-18 | Admin tier 1 keeps three placeholder KPIs (Present today, Overdue, Due today) beside the one real number, against the spec's "tier 1 is real numbers only" | Those four are the numbers the day is actually run on; dropping three of them until Phase 4 would teach the eye a layout that changes again |
| 0.5-19 | `Admin/Clients/Index.vue` gets the `DataTable` but **not** its sort headers or per-page control | `ClientController::index()` reads neither `sort` nor `per_page`; rendering controls the server ignores would be a lie in the UI. They turn on with one line each when Phase 2 teaches the controller |
| 0.5-20 | `Employee/Projects/Index.vue` keeps its card grid instead of moving to `DataTable` | An employee reads five projects by domain and deadline, not a spreadsheet; the table is for the admin who scans forty |
| 0.5-21 | Every brand value is read out of the logo SVG, never sampled from a raster; the Phase 0 navy/teal palette (decision 0-8) is retired | The Phase 0 palette was hand-derived and had no relationship to the client's mark |
| 0.5-22 | The brand splits into `--brand` (graphics only) and `--primary` (anything carrying text), and the split must not be collapsed later | `#F04E27` is **3.50:1** on the canvas — below the 4.5:1 AA floor, so it can never be a fill behind text |
| 0.5-23 | `#0A0D12` becomes the token `--brand-ink`, and every neutral moves to its hue **260.6** at chroma 0.006 (ramp 0.985 / 0.97 / 0.922 / 0.70 / 0.52 / 0.30) | The brand ships its own near-black; pulling the greys to that hue keeps them family with the mark instead of drifting blue at hue 223 / 237 / 250 |
| 0.5-24 | The dark canvas is `#0A0D12` exactly, not an approximation | It is the logo's own tile; an approximation would put the mark on a background that is nearly-but-not its own |
| 0.5-25 | `--card` / `--popover` stay pure white in light mode rather than taking the ramp's top step | Ramp step 1 (0.985) is already `--background`; a card at the same step would break the elevation rule ("one surface may not sit on another at the same elevation") and spec §3 T2 states cards stay white |
| 0.5-26 | `--ring` and `--chart-1` are `var(--brand)` | Focus ring and the series being read are exactly the "one important thing" `--brand` marks; both are graphics, not text |
| 0.5-27 | The Phase 0 teal is removed from the **content** tokens: `--accent-foreground` and `--secondary-foreground` move to the neutral ramp's 0.30 step | Teal text on a neutral hover fill was leftover Phase 0 decoration; the app should carry no teal outside the shell, which T2 replaces |
| 0.5-28 | `/profile`'s Login history keeps a hand-written `md:` card fallback instead of moving onto `DataTable` | It needs none of `DataTable`'s machinery, and `DataTable` renders its own `Card`, which would have put a card inside a card — the elevation rule again |
| 0.5-29 | The six slots of the 2FA code field are **one** tab stop (slots 2–6 are `tabindex="-1"`) | reka's `otp` mode bounces focus back to the first empty slot, which cancelled every forward Tab and left a keyboard user unable to reach "Use a recovery code". One tab stop is also how every OTP field a user has already met behaves |
| 0.5-30 | A skip link is added to all three shells, ahead of the spec, and it moves focus rather than only scrolling | WCAG 2.4.1 is Level A and the shells put 8–14 tab stops before the page body, reset on every Inertia visit. On the accountant dashboard it is currently the only way into the content at all |

### Measured contrast ratios

WCAG 2.x relative luminance on sRGB, after gamut clipping. Body pairs must reach 4.5:1;
graphics pairs are noted at their 3:1 floor. **No pair needed a lightness adjustment** —
every spec value passed as written.

The fuller table — 52 pairs including the ones below, measured by a script that parses
`app.css` itself — is `DESIGN.md` §2. This is the record of what T1 saw at the time.

| Pair | Light | Dark |
| --- | --- | --- |
| `--foreground` / `--background` | 13.06:1 ✅ | 17.84:1 ✅ |
| `--foreground` / `--card` | 13.63:1 ✅ | 16.25:1 ✅ |
| `--muted-foreground` / `--background` | 5.28:1 ✅ | 7.28:1 ✅ |
| `--primary-foreground` / `--primary` | 4.99:1 ✅ | 7.31:1 ✅ |
| `--primary` / `--background` (link text) | 4.92:1 ✅ | 7.31:1 ✅ |
| `--primary` / `--card` (link on a card) | 5.13:1 ✅ | — |
| `--primary-hover` / `--background` | 6.34:1 ✅ | — |
| `--sidebar-foreground` / `--sidebar` — **superseded** | 8.97:1 ✅ | 11.97:1 ✅ |
| destructive foreground (white) / `--destructive` | 4.76:1 ✅ | 2.89:1 ❌ raw / **6.00:1 ✅ as rendered** |
| `--destructive` / `--background` (`text-destructive`) | 4.56:1 ✅ | 6.73:1 ✅ |
| `--foreground` / `--brand-tint` (active row) | 12.31:1 ✅ | 14.45:1 ✅ |
| `--status-todo-fg` / `-bg` | 5.99:1 ✅ | 8.32:1 ✅ |
| `--status-progress-fg` / `-bg` | 6.43:1 ✅ | 8.38:1 ✅ |
| `--status-review-fg` / `-bg` | 6.08:1 ✅ | 8.20:1 ✅ |
| `--status-done-fg` / `-bg` | 6.26:1 ✅ | 8.65:1 ✅ |
| `--status-waiting-fg` / `-bg` | 6.18:1 ✅ | 7.87:1 ✅ |
| `--status-cancelled-fg` / `-bg` | 5.79:1 ✅ | 6.47:1 ✅ |
| `--brand` / `--background` (graphics, 3:1 floor) | 3.45:1 ✅ | 6.73:1 ✅ |
| `--primary-foreground` / `--brand` — **never rendered** | 3.50:1 ❌ | — |

The last row is the measurement that justifies decision 0.5-22: `--brand` is 3.50:1 under white
text, so it is graphics only. It is recorded as a failure on purpose — if a later task puts text
on a `--brand` fill, this is the number that says why it must not.

The sidebar row is marked superseded because T1 measured it against the Phase 0 navy it had
deliberately left alone (0.5-11). T2 then replaced the shell, and the same pair now measures
**13.06:1 light / 16.12:1 dark**. The old number is kept as the record of what T1 saw, not as a
current fact.

## Phase 2 (20 Sep 2026) — Tasks

| # | Decision | Reason |
| --- | --- | --- |
| 2-1 | Phase 0.5's design foundation is accepted as-is and Phase 2 builds on it unchanged | The user ran it on their own machine and approved it before any Phase 2 screen existed. Changing a token now costs one task; after ten screens it costs ten |
| 2-2 | Files go to the **local disk** behind signed, expiring URLs, not S3 — `FileService` and its tests are written against Laravel's `Storage` abstraction so the move is a `FILESYSTEM_DISK` change in `.env` | The user chose it. The spec calls for S3, but no bucket exists yet and the signing, validation, metadata and version-history behaviour is identical either way. The tests assert the behaviour, not the driver |
| 2-3 | Phase 2 runs to its gate without intermediate check-ins, as Phase 0.5 did | The user chose it |
| 2-4 | `BACKLOG` and `CHANGES REQUESTED` get their own status token sets (hues 196 and 344) rather than being mapped onto an existing tone | The spec has eight statuses; Phase 0.5 drew six before that list existed. A board where BACKLOG and TO DO are the same colour is a board that lies. Both new pairs measured over 4.5:1 in each theme, and both survive a deuteranope simulation against the existing six |
| 2-5 | **Follow-up, not fixed:** `review` (hue 75) and `waiting` (hue 50) are deltaE 0.16 apart under deuteranopia in light mode — effectively one colour to a red-green colourblind viewer | Found while placing the two new hues. It predates Phase 2 and re-tuning a shipped token is not this slice's business. It is survivable only because `StatusBadge` always prints the label, which is the evidence for the "colour is never the only carrier" rule rather than an exception to it. Worth its own brief |
| 2-6 | The employee Tasks table drops the Assignee column; the admin one keeps it | `Task::visibleTo()` already scopes an employee to their own tasks, so the column would print the reader's own name on every row. It is also why the employee surface offers no group-by-assignee: it would produce exactly one group |
| 2-7 | Grouped lists open from the top until ~15 rows are showing, then collapse the rest with counts | Eight expanded status groups is a wall and eight collapsed ones is a list that shows no list. The rule is a constant inside `DataTable`, not a per-screen prop, so Board and Calendar inherit the same answer instead of each inventing one |
| 2-8 | **Follow-up:** the Tasks list has no Assignee filter, because neither controller sends an employee list to build the picker from | `TaskService` already accepts `assignee_id`; only the props are missing. Shipping the control with an empty option list would be the "lie in the UI" that 0.5-19 forbids. Slice 2 adds the prop and the filter together |
| 2-9 | A task's `status` is guarded at the **model**: `Task` throws if `status` is dirty on an existing row and the write did not come through `applyTransition()` | The plan requires that every drag use the same checks as the form. A shared route is only intent — a future job, command or controller could still fill `status` from input. The guard makes the single path structural, and `applyTransition()` re-checks the transition map, so no path reaches an illegal status |
| 2-10 | "Explicit hand-off" is a recorded promotion of the other assignee to primary, with a required reason, audited as `task.reassigned`. The current primary may do it without a manager | The plan names hand-off without defining it. Storing the summary's author reduces completion to one question — is `work_summary_by` the primary's user — and the hand-off is the only thing that moves the subject of that rule. A primary handing their own work over before leave is the case it exists for; needing a manager would leave the task uncompletable until Monday |
| 2-11 | The original completion lives in columns on `tasks` (`first_completed_at`, `first_completed_by`, `first_work_summary`), not in the activity log | `activity_logs` carries a description string and nothing else, so the first completion could only survive there as prose — unqueryable, and "how many tasks were completed late and then reopened" becomes a string search. A log is what happened; the row is what is true. Reopen clears the live `completed_at`, since a row claiming a completion date while In progress contradicts itself |
| 2-12 | DESIGN.md §5.19 ("nothing routes a flash through the toaster") gains a sanctioned exception: a screen whose every write is a server round trip may claim the channel with `useFlashAsToast()`, which **moves** the flash rather than copying it | The task detail answers every write — including a refused transition — with `back()->with()`. The rule's intent is "announce once", not "never as a toast", and the collision was real: the first run showed the alert and the toast saying the same thing |
| 2-13 | The assignee list is filtered by holding `tasks.view`, not by role name or an exclusion list | The Accountant was being offered as an assignee for tasks they cannot open. A permission check means a future role that can carry tasks appears with no code change, and one that cannot never does — verified by moving the permission in both directions |
| 2-14 | The Tasks list tag filter shows globals plus the tags of every project `Project::visibleTo()` returns; a single task's picker shows only its own project's plus globals | Two different questions. The filter must cover every label a visible row could wear, or it hides work the list is showing; the picker must cover only what may legally go on that task. Sending every tag — which is what `index()` did — leaked other clients' project vocabulary through tag names on the Employee surface |
| 2-15 | **Follow-up:** a cross-project move drops the task's foreign *tags* but leaves its *dependencies*, so a moved task can still be blocked by a task in its former project | `addDependency()` refuses a cross-project pair, so the move can produce a state the validation exists to prevent. Found while building the move dialog; it needs a decision about whether the move detaches them, refuses, or warns |
| 2-16 | The Calendar's date window is an **interval overlap** — `coalesce(due_date, start_date) >= from AND coalesce(start_date, due_date) <= to` — not `whereBetween('due_date', …)` | A calendar draws a span, not a point. A task starting 28 Aug and due 3 Sep is being worked on at both ends and belongs on both grids; `whereBetween` on August drops it entirely, so the bar that should run off the right edge simply is not there. The `coalesce` also turns a task with one date into a single day rather than a NULL no comparison matches |
| 2-17 | Board and Calendar are **separate routes**, not a `?view=` parameter on the index | Each is bookmarkable, and each gets its own row in the permission matrix. A query-string variant would share one row and hide a surface from the route-coverage guard |
| 2-18 | A Board lane orders by `position` first; the List keeps ordering by due date. The ordering is a named parameter on `TaskService::query()`, not a fact about it | Two views, two questions. A list read top to bottom wants the soonest work first; a lane you drag wants the order you dragged it into. The Board originally reused the List's ordering, which put `position` third behind a date where it almost never broke a tie — so a card dropped at the top sprang back, and a manual order that does not visibly hold is worse than none |
| 2-19 | A lane is a **status**, not a `(project, status)` pair: `position` is scoped per status and a card may be reordered against one from any project | The plan's Board is "columns = statuses" across every project, and a card shows its project name because the lane mixes them. Per-project scoping meant a card could only be reordered against its own project's cards, and the screen had to walk a drop anchor backwards to find one — a workaround for a model that did not match the picture |
| 2-20 | **Follow-up:** `TaskResource::permissions()` resolves `can_review` per row, and `TaskPolicy::review()` resolves the reviewer per task — a query per card on the List, Board and Calendar | Inherited from slice 1 and harmless at 25 tasks. It is the thing that will bite first when a real board has hundreds. Wants a batched reviewer lookup, not a per-row gate call |
| 2-21 | **Follow-up:** an Admin board card carries the whole project fragment — client, internal notes, billing type, contract terms, finance — on every card, to print a project name | Correct privacy and correct `ProjectResource` behaviour, not a leak: an Admin may see all of it. But it is a lot of payload for one line of text, and finance detail sits in the browser on a view that never shows it |

| 2-22 | A tag's colour is the *name* of a status token (`backlog`…`cancelled`), not a hex value — enum, `Rule::enum`, and a CHECK constraint generated from the enum, with the picker's options sent from the server | Three places could hold the list: the enum, the constraint, and a Vue array. The third is the one that drifts, so there is no third — `GET …/tags` ships `colours`, and `statusToneClass()` turns a name into the triplet the rest of the app already uses. It also means a tag cannot wear a colour the design system has no token for |
| 2-23 | Deleting a tag writes the audit row **before** the cascade, while the pivot still exists | `task_tags` is gone by the time the delete returns, so the count of what it came off can only be taken first. An audit row saying "deleted" without saying what it cost is the kind of record you only notice is useless the day you need it |
| 2-24 | A conversation's membership is **computed**: `ConversationPolicy` asks the linked task's `TaskPolicy::view`, and `conversation_members` holds read state only | A stored member list is a second permission model that drifts from the first — reassign a task and the old assignee's row still says "member". A test reassigns a task and asserts the stale row buys nothing. `last_read_at` is the only thing the table is for |
| 2-25 | A file's signed `url` is a **policy check**, not a bearer token: `URL::temporarySignedRoute` into the app, never `Storage::temporaryUrl()`, and `local.serve` is turned off | A signature proves the link is untampered; it says nothing about who is holding it. Forwarding a signed bucket URL hands over the bytes. This way `FilePolicy::view` runs on every fetch, so a forwarded link is 404 for someone who may not see the owning record. `'serve' => false` removes Laravel's own bearer `GET /storage/{path}` route, which was unreachable but one `Storage::url()` call away from not being |
| 2-26 | `FileResource`'s `versions` key and `File::versions()` are **deleted**, and history is its own endpoint | `version_of` holds the root's id, so the relation hangs the chain off v1 — and `for()` returns only current rows, so the root is never in the list the moment a replacement exists. The key answered `[]` for every row a caller could hold, which meant replacing a file quietly buried every earlier version. A relation that is empty exactly when it matters is the trap that produced the bug, so it is gone rather than re-documented; `FileService::history()` reads the chain through `chainId()` and `GET {surface}/files/{file}/versions` serves it |
| 2-27 | `UpdateTagRequest` checks visibility in `authorize()`, ahead of its own rules | `rules()` builds the uniqueness scope from the routed tag, and a Form Request validates before the controller body — so a PUT naming a tag scoped to an invisible project answered 422 ("already exists in this scope") where `visibleTag()` would have said 404. No seeded role can reach that state today; the point is that absent must mean absent at the *first* thing that answers, not the second |
| 2-28 | Whether a screen offers the tag manager comes from the server (`canManageTags` on all six Tasks payloads), like `can_plan` before it | The first implementation read `auth.user.role === 'ADMIN' \|\| 'MANAGER'` — a second copy of `TagPolicy::manages()` living in a Vue file, where nobody will remember to change it. Same reasoning as 2-9: the single path has to be structural, not a convention |
| 2-29 | `ClientResource` carries a `permissions` block, as `ProjectResource` has since Phase 1 | The client Files tab had to hard-code `canAttach = true` on the reasoning that only an Admin reaches that surface. True today, and an inference either way — the day a role is added the screen goes on offering an upload the endpoint refuses |
| 2-30 | **Follow-up:** the task detail payload ships `attachments` (a full `FileResource` array) that no screen reads, because `FilePanel` fetches its own list | Two signed-URL mintings and two policy passes per attachment on every detail render. Not a leak and not a bug — `attachment_count` beside it *is* used — but the array has no consumer and should either be dropped or become what the panel paints from |


| 2-31 | A notification's recipients pass two filters and neither names a role: the **type's** required permission (`NotificationType::requires()`), and **`view` on the object itself** | The Accountant must never receive a task notification, and "do not send to the Accountant" is the wrong way to say it — a future role that should receive one would have to be remembered. Holding `tasks.view` is the actual reason, so that is what is asked. The object gate is the same 404-shaped rule the list endpoints enforce, applied to the mail |
| 2-32 | The one exception: a person **removed** from a task is told without the `view` gate | They were an assignee a second before the event, the payload carries the title and a project id and nothing else, and the alternative is that the one person who most needs to know is the only one never told — because the fact that would let them be told is the fact that just changed |
| 2-33 | Grouping happens at **dispatch**, and a row the recipient has already read does not absorb the next event | The spec says dispatch-time, so twelve comments inside the window are one row written with `count: 12`. The read rule is ours: without it, reading "3 new comments" silently swallows the fourth. Reading closes the group |
| 2-34 | `notifications` has `updated_at`; `audit_logs` deliberately does not | Not inconsistency. `hq_app` holds no UPDATE grant on `audit_logs` at all, so a column claiming when it was updated would be a lie. A notification is updated on both its hot paths — dedup incrementing `count`, and mark-read — so `created_at` alone cannot tell "this group started two minutes ago" from "and it is still growing" |
| 2-35 | Priority is `NotificationType::priority()`, not a column | The spec's own column list omits it, and two "in review" rows can never disagree about urgency. There is nothing per-row to store |
| 2-36 | Cancelling a project **prompts**; it closes nothing | Phase 1 could only record the intent as a string. The spec's word is "prompted", a status moves only through `transition()` (2-9), and twenty open tasks on a cancelled project are twenty separate decisions, not one bulk write nobody reviewed |
| 2-37 | `TaskBucket` is the single statement of each of the eight bucket predicates, and every arm is a model scope | A dashboard card's count and the list it opens have to be the same `where`, or the card is a number nobody can reconcile. Overdue in particular already had one definition (`Task::scopeOverdue()`) and this keeps it that way rather than adding a second in a controller |
| 2-38 | Due Today and Overdue are `?bucket=` links into My Tasks, not routes of their own | Same question, same tasks, narrowed by a date — and My Tasks already shows all seven counts at once, so landing on "Due today" still shows you what is late. Two more routes would mean two more controllers, two more matrix rows and two more places for "overdue" to drift |
| 2-39 | The bell polls every 15s and **stops when nobody is looking** — hidden tab, unmounted component, signed out | Realtime is Phase 6; until then this is the only thing in the app that runs without anybody asking. Proven by counting requests across visibility changes, sign-out and five rapid toggles rather than asserted |
| 2-40 | Marking a notification read is announced by nothing | It would be a notification about a notification, invented by the screen, on the one surface whose whole subject is being told things. The row and the badge going quiet is the feedback, and `back()` re-renders both from the server — which is also what makes "never decrement the count locally" structural rather than a rule someone has to remember |
| 2-41 | The Notification Center is the **same route** that answers the bell's JSON: `GET /notifications` renders the page for a browser and the identical array for a fetch | A second route rendering a page that then fetched this one would have meant another controller method, another matrix row, and two owners of "which tab am I on" — the tab is a bookmarkable query parameter, and a page re-reading it client-side would hold it twice |
| 2-42 | **Follow-up:** the Accountant costs one `403 /notifications/recent` per full page load before the bell removes itself | Deliberate — deciding it in Vue would mean naming a role, which is exactly what `NotificationPolicy` refuses to do. Avoidable by sharing the mailbox permission in `HandleInertiaRequests`, which would change every page's prop shape and the tests that assert it |
| 2-43 | **Follow-up:** `Pagination.vue` draws Laravel's `…` ellipsis entry as a filled, active-looking pill | It filters on `link.page !== null` and the ellipsis row has no `page` key at all, so `undefined !== null` lets it through. Pre-existing, visible past about ten pages, and it affects every paginated list |
| 2-44 | **Follow-up:** `relativeTime()` now exists in four places | Two project/client screens, the sessions card, and `notifications.ts`. Each followed the repo's precedent of keeping its own formatters beside its types; four is where that stops being a precedent and starts being a copy. It belongs in `lib/` |


| 2-45 | `TaskCompleted` notifies the **assignees** as well as `created_by` and the reviewer, widening the spec's own recipient list | The plan contradicts itself: the Phase 2 Backend paragraph lists `created_by` + reviewer, and the same phase's "Done when" says a notification appears at every step. On this team those two people are usually one person who is also the actor — and the actor is always dropped — so an approval wrote **zero** rows and the person who did the work was never told it was accepted. The acceptance sentence wins, because it is what "done" means; the list reads as an implementation note written before anybody walked the flow. Nobody learns anything they could not already see by opening the task |
| 2-46 | `TaskSubmittedForReview` has a plural branch: a resubmission reads *"…is waiting for your review **again**"* | A second submission groups into the reviewer's existing unread row (2-33), so without one the sentence still described the first submission and the only sign anything had changed was `count`, which the screen deliberately does not print. A reviewer who had already asked for changes saw a bell saying exactly what it said an hour ago |
| 2-47 | The seeded task is renamed to **"Optimize Home Model pages"**, matching the acceptance sentence verbatim | The plan names that task; the seed had "Rewrite the Home Model page titles". The close-out walk had to stop and decide which row was meant. Renaming the demo row is free; amending the plan's acceptance sentence is not |
| 2-48 | **Follow-up:** a handled review notification stays unread | After a reviewer rules on a task, *"…is waiting for your review"* is still unread in their bell. Nothing resolves a notification when its subject is dealt with. Wants a rule about what closes a row, not just what opens one |
| 2-49 | **Follow-up:** the reviewer's reason never reaches the notification | *"…moved to Changes requested"* carries no reason; it lives on the activity trail only, at the bottom of the panel. The payload already has a `context` block carrying `to_label`, so this is additive |
| 2-50 | **Follow-up:** the Admin dashboard's "Needs your attention" panel and its "Tasks by status" donut have no feed | The controller sends no items and `:data="[]"` is hard-coded, so both show their empty state directly under cards counting 6 overdue and 1 awaiting review. Phase 10 finalises dashboards; until then the panel's subject is real data and its emptiness reads as a bug |
| 2-51 | **Follow-up:** the Files tab is not in the URL on project and client detail | `tab` is a local `ref`, so a reload drops back to Overview and a colleague cannot be linked to a project's files |

## Phase 3 — recurring tasks

| # | Decision | Reason |
| --- | --- | --- |
| 3-1 | The **partial unique index** `(recurring_task_id, recurring_period) where recurring_task_id is not null` is the duplicate prevention; the engine's check is only the nice error | Two schedulers, a retried queue job and a manual "Generate now" on the same minute must not produce two October tasks, and an `if` cannot promise that. `TaskService::create()` writes both columns in the same INSERT as the task (a create-only `BIRTH_FIELDS`, so an update cannot restamp a period), so there is no window between the check and the row. A test writes the competing row from a `Task::creating` listener — strictly after the check, strictly before the INSERT — and asserts the message only reachable from the catch block |
| 3-2 | The index deliberately does **not** exclude `deleted_at` | A soft-deleted instance still occupies its period. Excluding it would let tomorrow's 00:05 run resurrect a task somebody deleted on purpose. `instanceFor()` uses `withTrashed()` so the check and the index agree exactly |
| 3-3 | A period key is one function with one `match`: `2026-10`, `2026-W41`, `2026-10-07` | Two properties carry weight and are tested: a key is unique within one template (a template has one frequency, so its keys are all one shape), and keys of one shape sort lexicographically into chronological order — which is why "is last period's instance still open" is a plain `where recurring_period < ?` and not a second date column. The weekly key uses the ISO year, so 29 Dec 2025 is `2026-W01` rather than sorting a year out of place |
| 3-4 | **"Ended" means the project's status is no longer open** — Completed, Cancelled or Archived. On Hold keeps generating, and a passed `deadline` is not ended | The spec's stop list never names On Hold, which means "paused and coming back". And every seeded retainer carries a rolling deadline a few days out, so treating a slipped date as the end would silently stop every retainer in the agency inside a week. The stop is derived on every run rather than flipping `active` off, so unarchiving a project resumes it by itself |
| 3-5 | Generating a task adds **no** new notification type | `TaskService::create()` already fires `TaskAssigned`. A second row for the same act would break §11's "one event = one notification" |
| 3-6 | A migration rewrites the `notifications_type_is_known` CHECK from the enum whenever a phase adds a type | Phase 2 generated that constraint at create time, so a Phase-2-era database would have refused the first due-tomorrow reminder at 08:00 the morning after deploy while every test passed. **This is a trap that will be stepped in again** — every future phase adding a notification type needs the same migration |
| 3-7 | **Follow-up:** `hq:notify-due-tomorrow` dedups "ever", not per due date | A task moved from Friday to Monday is "due tomorrow" a second time and gets no second reminder. It is the same trade `hq:flag-overdue` already makes, mirrored on purpose rather than inventing a second dedup rule — but it is a real limitation |
| 3-8 | The recurring screens are **Admin-only**, and every non-Admin cell in the matrix is **403**; **404** is reserved for the record | A Manager owns the tasks a template produces, but a standing monthly instruction is a decision about the engagement, not about this week's work. The refusal is about what they may do, not which shell they arrived in — `surface:admin` and `RecurringTaskPolicy` both say no. The 404 branch re-resolves every per-template lookup through `RecurringTask::visibleTo()` *before* the policy, so a template on an invisible project is absent; no matrix row can show it today, because an Admin sees every project, so it is asserted directly instead |
| 3-9 | The recurrence editor sends `frequency` plus that frequency's parameters; one server-side `match` picks one of `RecurrenceRule`'s three constructors | Nothing in the Vue does month arithmetic, ISO weeks or day counting — the next run, its period, the period's bounds and the due date all come back from a preview endpoint, which is `RecurrenceRule` answering. The unused frequencies' values ride along and are ignored, so toggling the editor twice is not a validation error |
| 3-10 | The form re-states the rule's bounds instead of relying on it | `RecurrenceRule` *clamps* an out-of-range day, which is right for a command running at 00:05 and wrong for a form: somebody typing 31 would silently get 28 |
| 3-11 | `RecurringTaskEngine::stopReason()` became public | The screen asks the same method the 00:05 run asks. The alternative was a second copy of the plan's §21 stop rules in a controller, or a screen that prints only the last log row's message — which says nothing at all for a template whose project was cancelled this morning |
| 3-12 | **Follow-up:** a template can be switched off but not deleted | The spec names an active toggle and no delete, so one set up by mistake stays on the list forever. Worth a client decision |
| 3-13 | **Follow-up:** a template's name on task detail still carries its placeholder — "Buffalo Modular Monthly SEO — {period}" | Correct, since that *is* the title pattern, but it reads oddly beside the rendered title. The alternative is inventing a second "display name" field, which is a decision rather than a fix |


| 2-52 | **Resolved is a separate column from read.** `notifications.resolved_at` is new; `is_read` is untouched | They mean different things and mixing them would have broken 2-33. `is_read` means *you looked* and closes the group; `resolved_at` means *you dealt with it* and only quietens the row. Collapsing them would have closed the group too, so a resubmission would have started a fresh row of one and told the reviewer, for the second time, exactly what it told them the first — which is the bug 2-46 exists to prevent. Growing a row clears `resolved_at`, so the resubmission re-enters the badge |
| 2-53 | What resolves a notification is a fact about the **type**, listed beside `priority()` and `tab()`, and only the **actor's own** rows resolve | `TaskSubmittedForReview` is answered by either review verdict; nothing answers `TaskCommented` — reading a comment is not answering it. And two admins can both be reviewers, so one ruling must not empty the other's bell: they have not read it. The rule is applied in `NotificationService` *before* the empty-recipient return, because on this team the approver is usually also the only recipient, so his approval writes zero rows — resolving hung off a row being written would have missed the exact flow 2-48 was raised about |
| 2-54 | The reviewer's reason reaches the singular sentence only, whitespace collapsed and capped at 120 characters | A grouped row stands for several moves and cannot carry several reasons; printing the last one would attach it to a sentence about all of them. The cap is about what gets pushed out of view, not about the layout breaking — measured at 360px, a 500-character reason makes a 360px-tall row in a popover that shows ten |
| 2-55 | **Follow-up:** an assignee moving a task back out of review leaves the reviewer's row open | *In review → In progress* is a legal move for the assignee, so the review request is moot — but the actor is not the reviewer, and 2-53 says one person's act may not empty another's bell. Closing it needs a rule about the **object's state** rather than about who acted, which is a different shape |


## Phase 4 — time and attendance

| # | Decision | Reason |
| --- | --- | --- |
| 4-1 | `time_entries` has **no `state` column**: running, paused and stopped are read off the two timestamps that cause them | A `state` string would be a second statement of the same three predicates (2-37), and it would first disagree with them the day a watchdog stop forgot to keep them in step. The scopes are the single spelling, and the partial unique index is written against the same predicate |
| 4-2 | **One open timer per employee is a partial unique index**, `where ended_at is null`, with paused counting as open | The widget, a replayed offline batch and the watchdog can all reach the same employee in the same millisecond, so an `if` cannot promise it. The check above it exists only to produce a readable sentence. Paused counts as open because a second session beside a paused one is how an afternoon gets counted twice. Same reasoning as 3-1 |
| 4-3 | On replay, **only the heartbeats are evidence**, and nothing in the service increments | Every write sets `ended_at`, `paused_seconds` and `duration_seconds` to absolute values computed from timestamps, so the same batch landing five times leaves the row in the same state five times. The entry never ends later than the last heartbeat, which is what "server wins" means in practice: a client claiming six hours with nothing after 13:04 to show for it gets 13:04 |
| 4-4 | The watchdog's **rule 2 fires only on unflagged entries** | Otherwise it fires again a minute after the employee resumes, and a safeguard that cannot be acknowledged is one people route around. The flag stands; the rule does not repeat |
| 4-5 | Rule 1 runs before rule 2 | An entry both silent and overlong is a closed laptop, and the true account of a closed laptop ends at its last check-in, not at the maximum session length |
| 4-6 | A timer flag is a **sentence carrying its threshold and its moment**, not a code | "Stopped automatically: the timer stopped checking in for more than 5 minutes, so this entry ends at its last check-in, 1:04 pm." A flag nobody can read is a flag nobody investigates |
| 4-7 | Totals ask exactly one question: `approved_at is not null` | An auto entry is approved by the system at stop; a manual one only when `manual_time_requires_approval` is off. One nullable timestamp, no status enum that can contradict it. **Pending hours are reported separately and in words** so that nothing a person actually worked is invisible while it waits |
| 4-8 | `tasks.tracked_seconds` became a **recomputed cache**, never incremented | It is refreshed from `time_entries` on every stop, manual entry and edit. Phase 2's seeder wrote fabricated figures onto about twenty tasks, so the first real session on one of them visibly replaces the fabrication with the truth — correct, and worth knowing before it surprises somebody at a demo |
| 4-9 | `attendance_records` is unique on `(employee_id, date)`, and **Off Day and Remote are not storable statuses** | Three writers reach the table and one of them is an unattended 23:55 sweep, so check-then-insert is not a promise anyone can keep. And a stored Off Day would outlive the schedule it was copied from — it is derived from `working_days` every time it is asked |
| 4-10 | An employee with **no `start_time` cannot be Late** | Lateness is a comparison against a scheduled start. Where there is none, the honest answer is Present, not a default hour invented by the system |
| 4-11 | **Tapu can never be marked Absent**, and that is structural rather than a matter of discipline | Both the policy and the service require `tracking_mode = office_attendance` before a row can be written, so no hand — Admin, command or seeder — can write one. A remote employee's day is Remote; his minutes come from `time_entries` |
| 4-12 | Phase 5's leave/holiday skip plugs into **one method**, `coveredByLeaveOrHoliday(Employee, date)`, which returns false today | Called from exactly one place in `markAbsent()`. Phase 5 replaces its body; the command, its query and every test that does not name leave stay as they are. A test proves the seam by faking that method's answer rather than by reading the file |
| 4-13 | The attendance **month grid becomes a list below `md`** | Measured, not stylistic: seven columns at 360px gives about 48px of cell, and every status here must print its **word** — DESIGN.md §2.2 measures four of eight status tones failing 3:1 in light without the label, and §1.4 has a deuteran pair at ΔE 0.16. "Half day" does not fit in 48px at any legal type size, so a phone grid would have had to drop the word or the date |
| 4-14 | `attendance.edited` and `schedule.changed` were added to the audit events, although Part C §4's list names neither | Treated as an omission rather than a decision: an attendance row is what Phase 9 pays from, and a schedule change silently relabels a fortnight of past arrivals as late or on time. `time_entry.edited` logs **every** edit, not only an Admin's — a trail that depended on who was signed in would be the wrong trail |
| 4-15 | Clocking in lives in `routes/shared.php`, not per surface | Part D §8 is explicit that both Admins clock in too, so it is a fact about the person rather than the shell they are in — the same reasoning as the profile and the file download. It also meant `routes/employee.php` needed no change, which is one fewer contested file between two concurrent agents |
| 4-16 | **Follow-up, and the one that will bite at GATE C:** there is nowhere to approve a pending time entry | `manual_time_requires_approval` defaults on, so a manual or edited entry is written unapproved and does not count. The only screen that could sign it off is Admin → Workforce → Time, which is the next slice. Until then those hours are reported in words but genuinely uncounted |
| 4-17 | **Follow-up:** `tests/Unit/SurfaceTest.php` asserts a hard-coded count of audit-event cases | Every phase that adds an event must bump it, and two concurrent agents both had to this round. Brittle, and it should assert the set rather than the size |
| 4-18 | Rejecting a time entry **keeps the row, its hours and the employee's own reason**, and adds `rejected_at`, `rejected_by`, `rejection_reason` with a CHECK that approved and rejected cannot both be set | A refusal that deleted the row would leave the person who recorded the hours with no trace of what happened to them. A rejected entry counts exactly as little as it did while waiting — `approved_at` stays null, so 4-7's single predicate learns no new word — and their Time page says "Turned down: <reason>". Approving a refused entry clears the refusal rather than layering on it |
| 4-19 | `daily_work_summary` is a **FULL OUTER JOIN of two CTEs**, one per source, with a `source` column naming which halves are present | Part C rule 6 forbids merging the two tables; a view reports across them. Full outer rather than left, so neither source can disappear. It can say `both`, which `tracking_mode` should make impossible — but a report that silently dropped half of such a day is exactly the bug rule 6 exists to prevent, so the view names it instead of hiding it |
| 4-20 | `trackedMinutes()` costs **one query per roster or month, not one per row** | It is called per cell. A request-scoped cache primed by `roster()` and `month()` with one grouped query keeps the seam's signature unchanged, and a test asserts through the query log that a 30-day grid costs 1 query, not 30 |
| 4-21 | Approving is refused to the entry's own owner, and gated on `attendance.manage_others` rather than a new permission key | Tested by granting the remote role that key and asserting Tapu still cannot rule on his own entry but can on somebody else's — so the refusal is about ownership, not about a missing key |
| 4-22 | The Timesheet's week starts on **the first working day in that employee's schedule**, falling back to Sunday and never to Monday | The seeded week is Sun–Thu. The test that proves it asserts three schedules against one Thursday, including Tue–Thu — a hard-coded Monday passes a Mon–Fri case and fails that one |
| 4-23 | The Timesheet becomes a **day-major list below `md`**, and Workload's estimated-vs-tracked is two untinted columns that are never divided, subtracted or sorted | The grid has nine columns where 4-13's had seven: at 360 that leaves about 23px a cell against the ~40px the shortest real value needs. And Part H forbids a productivity score — a ratio, an arrow or a sort by "worst" would be one, so the service emits nine fixed keys, no column is sortable, and people are ordered by name |
| 4-24 | The Timesheet reads `time_entries`, never `tasks.tracked_seconds` | That column is a recomputed cache (4-8) and Phase 2's seeder wrote fabricated figures into it. A test writes 99999 there and asserts the screen ignores it |
| 4-25 | **Follow-up:** `TimerService::edit()` re-approves on the spot when `manual_time_requires_approval` is off, clearing a refusal | Correct per Part D §21, but it means an Admin's "no" can be undone by the employee editing the entry while that setting is off. Worth a client decision |
| 4-26 | **Found by reading, not by measuring:** the admin Attendance and Schedule pages shipped with **no layout at all** — no sidebar, no top bar, no skip link | Every accessibility measurement on them passed, because a page with no shell has nothing to overflow and almost nothing to tab through. A measurement only answers the question it was asked; "is this page inside the application" was not one of them |


## Phase 5 — leave and holidays

| # | Decision | Reason |
| --- | --- | --- |
| 5-1 | A **holiday is derived, never stored**: `holiday` was removed from `AttendanceStatus::storable()` and the CHECK rewritten from the enum | Decision 4-9's rule applied a third time. A stored Holiday would outlive the calendar it was copied from, so adding or removing one must change what a *past* day shows — there is a test that adds one, asserts the change, deletes it and asserts it back. `Leave` deliberately stayed storable: it records a decision about a person, not a fact derived from a shared calendar |
| 5-2 | `dayFor()`'s order is: a record wins → **Off Day** → **Holiday** → Remote → nothing yet | Off Day first, because a public holiday on somebody's own non-working day is not news and the more specific statement about *their* week wins. Holiday before Remote, because the company calendar applies to everybody however their work is tracked — so on Victory Day Tapu reads Holiday, with his tracked minutes riding alongside |
| 5-3 | `holidays` has no `year` column and is unique on **`(date, name)`**, not on `date` | A year is a range over `date`; a column would restate it. And two holidays genuinely share 1 May in the seeded year, so a unique `date` would have made the seeder silently drop one |
| 5-4 | The seeder plants the current year **only if that year is empty**, and knows no future year | A per-row `firstOrCreate` would resurrect a holiday the Admin deleted, every morning, because the launcher seeds on every start. And Bangladesh gazettes the next year late in the preceding one, so a generated 2027 would be this file's guesses wearing a seed's authority. Next January it seeds nothing and says so; the screen's empty state asks the Admin to type the gazette in |
| 5-5 | Each seeded date carries `certainty: fixed\|moving`, and the seeder **prints the 15 moving ones when it runs** | Fifteen of twenty-one are lunar and move each year. A comment in a file nobody opens is not a warning; the console is where the person seeding actually is |
| 5-6 | `leave_types` has **two** booleans — `has_balance` (capped) and `is_unpaid` (costs pay) — not one | They coincide only on Unpaid. **Other is uncapped and still paid**, so a single flag read as "uncapped means unpaid" would have docked a day of Other leave and nothing would have said so until a payslip did |
| 5-7 | Overlapping requests are refused by a **GiST exclusion constraint**, not a check in the service | `EXCLUDE (employee_id WITH =, daterange(start,end,'[]') WITH &&) WHERE status IN ('pending','approved')`. Two tabs, or a retry, can file the same week twice in the same millisecond; 3-1 and 4-2 again |
| 5-8 | A leave request's status is guarded at the **model**, exactly as a task's is (2-9), and there is **no `withoutStatusGuard()`** | Approval decrements a balance, writes attendance rows and stores what Phase 9 pays from. A second path would produce a request saying *approved* with none of that behind it, and nobody would find out until a payslip was wrong. `Task` has an escape hatch for its demo seeder; the leave seeder creates no requests, so this one has none |
| 5-9 | Approving leave for a **remote-timer employee writes no attendance row at all** | Decision 4-11 held rather than worked around: an approval quietly becoming the one hand that can write Tapu a row would take "Tapu is never Absent" apart from the inside. His balance, `unpaid_days`, calendar, task flag and sweep-skip all work identically |
| 5-10 | On approval, a day whose row has **no clock-in** is claimed; a day somebody clocked into is left alone | The first is the Absent the 23:55 sweep wrote, which a retro-approval should correct. The second is evidence that a person was at work, and leave does not erase it |
| 5-11 | The "assignee on leave" flag is **one `addSelect` in `TaskService::query()`** — the single funnel for List, Board, Calendar, My Tasks and the dashboards | Every view gets it with no extra query (asserted: a full task list is still one select on `tasks`). An approver sees every assignee's; everybody else only their own. **No reassign control anywhere**, and a test asserts the assignee set and the primary are byte-identical before and after approval — Part D §5 says flagged, not reassigned |
| 5-12 | Leave lives in `routes/shared.php`; the Accountant applies in their own shell | Part C §1 gives *apply for own leave* to every role. Decision 4-15's shape exactly, and it left `routes/employee.php` and `routes/accountant.php` untouched — one fewer contested file between two concurrent agents |
| 5-13 | The leave calendar becomes a **list of the days somebody is away** below `md` | Measured: a seven-column cell is 40px at 360 against the 184px the row's badge wants. Decision 4-13's attendance cell had ~48px and could not fit one status word; a leave cell must fit a name *and* a word. The list also skips empty days, which a grid cannot |
| 5-14 | **The Accountant now has a mailbox**, and nothing was carved out to give them one | `leave.apply` is a key Part C gives every role, so `NotificationPolicy::viewAny` simply stops refusing them. Their notification cells move 403 → 200/302, and `POST /notifications/{n}/read` moves 403 → **404**, which is the privacy rule getting *stronger*. Decision 2-42's follow-up closes by itself |
| 5-15 | Holiday writes audit as `ConfigurationChanged` and is gated on **`settings.manage`**, not `leave.approve` or `attendance.manage_others` | Both of those are *own team* for a Manager, and a company-wide calendar has no team — either would have quietly handed a team manager the agency's calendar. Part C §4 already names "configuration changes", so no new audit case was invented either |
| 5-16 | **Found the hard way:** two test files both defined `ENDPOINT_MONDAY`, and Pest declares a test file's constants **globally** | One meant 2026-09-14 and the other 2026-10-05; whichever PHP loaded first silently won. Each file passed alone and the suite failed — the worst shape a failure takes. A duplicate is a PHP *warning*, not an error, so nothing stopped it. Test constants are now folder-prefixed, and `AGENTS.md` says so |
| 5-17 | **Follow-up:** a leave day that is also a company holiday still counts against the balance | The attendance row is skipped, but `days` and `unpaid_days` still count it, so somebody burns Annual leave on Eid. Deducting it would make a stored number disagree with the same computation after an Admin edits the calendar — a client decision, not a quiet fix. It also needs a holiday-*only* predicate; `coveredByLeaveOrHoliday()` answers "holiday or approved leave", which is right for the sweep and wrong for counting |
| 5-18 | **Follow-up:** `daily_work_summary` does not know about leave or holidays | Part D §20 says the view unions attendance, time entries, leave and holidays; it reads the first two. **Phase 9 payroll reads this view**, so it needs one migration covering both before then |
| 5-19 | **Follow-up:** there is no way to withdraw a leave request | Part D §9's status list has four and "cancelled" is not among them, so somebody who books the wrong week must ask an Admin to reject it |
| 5-20 | **Follow-up:** the "menu item opens a confirm dialog" pattern drops focus to `<body>` app-wide | reka restores focus to the menu item, which the menu has already unmounted. Fixed locally on the Holidays screen; `Admin/Projects/Index.vue` still has it and anything copying that pattern inherits it |


## Phase 6 — communication and realtime

| # | Decision | Reason |
| --- | --- | --- |
| 6-1 | **Decision 2-24 generalised to all five conversation types rather than bending.** Nothing anywhere reads `conversation_members` to decide who may do anything | `task` asks `TaskPolicy::view`, `project` asks `ProjectPolicy::view`, `team` and `announcement` ask for the `messages.use` key, and a DM asks whether you are one of the two people named **on the conversation row**. `last_read_at` is still read state and grants nothing, in all five |
| 6-2 | A **DM's pair lives in two columns on `conversations`** (`dm_one_id < dm_two_id`), not in two member rows | Phase 2's own note had assumed member rows would be the grant for DMs — which would make that table mean *grant* for one type and *read state* for four, the exact ambiguity 2-24 exists to prevent. Two ordered columns buy three things instead: a DM cannot grow a third person because there is nowhere to put one; "the DM between A and B" becomes a unique index, so opening one is a race-free `firstOrCreate`; and the 2-24 test generalises — a stale member row on a DM still gets 404 |
| 6-3 | `messages.use` is a **new permission key**, the 24th | Part C §1's matrix has no messaging row at all, and Phase 6 says only that the Accountant has no messaging access by default. Borrowing `tasks.view` was rejected: its holders are the same four people today, but that key means "you may see tasks", and a future role with tasks and no chat would then have to be carved out by name — which is what 2-13 and 2-31 refuse |
| 6-4 | **An ordinary channel message notifies nobody.** DMs, @mentions and announcements do | Fifteen people with the Messages page open all day would stop reading a bell that fired per channel line. The channel's unread count is where that belongs — and the silence is exactly what makes an @mention worth typing |
| 6-5 | **A mention suppresses the comment notification for that person**, at the recipient list | Somebody both mentioned in and assigned to the same message gets one row, the mention. Two rows in one bell about one sentence is saying the weaker thing twice. Dropping them from the candidate list rather than suppressing the type means everybody else on the task is still told |
| 6-6 | A mention is **not a grant**: a mention row is written only for somebody who can already read the conversation *and* whom the body actually names | So `message_mentions` always agrees with what every reader of the thread can see. An id without the text writes nothing; a name in ordinary prose writes nothing |
| 6-7 | **A channel's broadcast-auth callback asks the policy that already exists** — `ConversationPolicy::view`, `TaskPolicy::view`, `NotificationPolicy::viewAny` | A socket is a second door into the same rooms, so it is gated by the same lock. The auth route runs this app's full stack (`web, auth, active, two-factor`), not Laravel's default `web` |
| 6-8 | The **polling fallback is a supported mode, not dead code**, and its payload is the *same* assembly | `NotificationService::feed()` builds it once; the controller returns it and the broadcast event broadcasts it. A test compares the two with `toBe()`, not "same shape" — which is how a real regression was caught: `NotificationResource` resolves a deep link off `$request->user()`, and a broadcast assembled in a queued job had no request, so every link came back null and the socket's payload was quietly the poorer of the two |
| 6-9 | The bell's 15-second poll (2-39) is **kept, not deleted** | It is now two things: the whole bell on a polling build, and the fallback while the socket is down on a socket build. The first read is always HTTP on both transports, because a socket delivers *changes* and the bell arrives needing the current state. A drop brings the poll straight back and the popover says so in words; a reconnect re-reads once, because anything that happened while the socket was down was delivered to nobody |
| 6-10 | The **Team directory ships ten keys and no salary or tracking field**, guarded three ways | The exact key list, a recursive forbidden-key walk over the members payload, and "no numeric value under any key but `id`". Plus an assertion that the order is **alphabetical** — an order is a ranking the moment it is by anything a person could do better or worse (Part H §1) |
| 6-11 | The Accountant **appears in the Team directory but gets no DM button**, and reads "Not tracked" rather than "Off day" | They work here and a colleague should be able to find them; a control the endpoint would refuse is not drawn. And they have no schedule, so `dayFor()` would call every day of their life an Off Day — true about an empty schedule, false about the person |
| 6-12 | `laravel-echo` and `pusher-js` are **dynamically imported** | A polling deployment then downloads no socket client at all: the layout chunk every page loads went from 68.1 KB to 49.0 KB gzipped |
| 6-13 | **Reverb binds to 127.0.0.1, not `0.0.0.0`**, and `allowed_origins` is the `APP_URL` host rather than `*`. `config/broadcasting.php` carries three connections only — `reverb`, `log`, `null` | Nginx is the only thing that should reach the websocket. Pusher, Ably and Mercure were removed on purpose: each is one `.env` line away from sending this agency's notifications to a third party |
| 6-14 | `daily_work_summary` now unions **four** sources, and leave is a **FULL OUTER** join | Closes 5-18 before Phase 9 needs it. Leave has to be a full outer because of 5-9: approving leave for a remote-timer employee writes no attendance row and he files no time entry, so his leave day has neither other half — under a LEFT join it would simply not be in the view, and payroll would read the month with it missing. `source` grows a fourth answer, `leave`. Holidays are a LEFT join on the **date alone**, aggregated first because two can share one date (5-3) and a naive join would double every employee-day it touched — a doubled row here is doubled pay |
| 6-15 | **A measurement hazard, found twice independently and now in `AGENTS.md`:** a focus ring *fades in* | Every shadcn control carries `transition-all`, so a computed style read in the same tick as a `Tab` samples the transition's start and reports a painted ring as missing. One agent turned 13 "failures" into 0 with a 300ms settle; the other turned 26 into 0 with 260ms. Combined with the known false pass (a Tailwind ring is a list of shadows whose first layer is transparent, so `box-shadow !== 'none'` passes a ring that paints nothing), **every earlier focus measurement in this repo that did neither is unreliable in both directions** |
| 6-16 | **Follow-up:** "may this person be sent a DM" is written twice | `active + messages.use`, once in `MessageController::messageable()` and once in `TeamMemberResource::dmUrl()`, because two agents needed it in the same hour and neither owned the other's file. It wants a `ConversationPolicy::dm(User, User)` ability both call |
| 6-17 | **Follow-up:** in polling mode `POST /broadcasting/auth` says yes to everyone | Laravel's `log` and `null` broadcasters override `auth()` with an empty body. It hands back nothing and no browser on a polling build calls it, so it leaks nothing — but it is why the matrix rows read 200, and it is Laravel's behaviour rather than a choice of ours |
| 6-18 | **Follow-up:** the announcement banner is on the Messages page only, not app-wide | The plan says "banner + notification". A global banner needs a prop in `HandleInertiaRequests`, which changes every page's prop shape and the tests asserting it — 2-42's warning. The High-priority bell row plus the Messages banner is what shipped |
| 6-19 | **Follow-up:** adding Reverb moved the lockfile further than a phase usually does | `guzzlehttp/guzzle` 8.2.0 → 7.15.5 (Reverb needs psr7 ^2.6; the lockfile was pinned at 3.1.0) and `laravel/framework` 13.32.0 → 13.33.0. Guzzle 7.15 is current stable, the AWS SDK supports it, the PHP 8.3.0 platform pin survived, and the suite is green — but it is worth knowing it happened |


## Client change requests (22 Sep 2026)

| # | Decision | Reason |
| --- | --- | --- |
| C-1 | The product is **goodERP**. The name is read from `APP_NAME` everywhere — `config('app.name')`, the shared `app.name` Inertia prop and `VITE_APP_NAME` — never written as a literal in a component | All three channels already existed and were all being ignored by components carrying the string. The next rename is one line in `.env`, not a search for the one somebody forgot. The logo mark is unchanged at the client's request, and the demo project "GoodTechies HQ — Internal" keeps its name: it names the agency's own internal project, not the product |
| C-1a | `APP_NAME` is set by the **launcher scripts**, not only in `.env`, and the browser title now reads the shared `app.name` prop instead of `VITE_APP_NAME` | The rename landed in the repo and did not appear on the client's machine: `.env` is gitignored, so it never synced, and the file bridge refuses to write it because it holds the database and seed passwords. A process environment variable beats `.env`, so the launcher makes the name take effect with no edit to a secrets file. Dropping the `VITE_APP_NAME` read also removes a build-time dependency — the name no longer needs a rebuild to change |
| C-2 | The **Board** is the default Tasks view; List and Calendar stay one click away on the switcher | The client asked for it. It changes where the nav points, not which views exist — `/admin/tasks` still serves the List |
| C-3 | `NavItem` gains `activePrefix`: where a row points and what it counts as "here" are separate questions | C-2 alone would have unlit the Tasks row on the List and Calendar pages. Any future row pointing at a sub-view needs the same, and the breadcrumb matcher uses it too |
| C-4 | ~~The Board strip fills the viewport~~ **Reverted 22 Sep.** The client changed their mind; the board grows to its contents and the page scrolls, as it did before | Built, verified, then withdrawn. Kept in the log rather than deleted, because the complaint it answered was real — at 1280 the horizontal scrollbar sits about 900px below the fold — and whoever meets that next should know it was solved once and by what: a measured strip height with lanes scrolling internally, in `TaskBoard.vue` at commit `1148417` |
| C-5 | ~~Drag-to-pan on empty board background~~ **Reverted 22 Sep**, with C-4 | Same change, same reversal. The working implementation is in `1148417` if it is ever wanted back |
| C-6 | **Bug found via C-4**, from 2-19: making a lane global left every seeded card sharing `position = 1000`, because the seed numbered per project. `column()` also ordered by `(position, id)` while the Board drew by `(position, due_date, id)` | Two failures in one. `place()` turns the anchor into an index in `column()`'s list, so a list in a different order from the screen drops the card in the wrong place; and tied positions leave no gap to drop into. `column()` now uses the Board's own ordering, and the seeder spreads positions within a lane — only where they collide, and never on re-seed, so the launcher's seed-every-start cannot undo a dragged card |
| C-7 | Position tests assert **order**, not absolute numbers | Four asserted values computed as if a lane were one project's. Sparse numbering is an optimisation — a lane out of gaps renumbers — so the contract is the order, and a test naming 1500 was testing the optimisation |

